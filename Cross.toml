[target.x86_64-unknown-linux-musl]
pre-build = [
    "apt-get update -qq",
    "apt-get install -y --no-install-recommends cmake clang llvm",
]

[target.aarch64-unknown-linux-musl]
pre-build = [
    "apt-get update -qq",
    "apt-get install -y --no-install-recommends cmake clang llvm",
]
